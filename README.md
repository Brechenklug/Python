# 🐍 Python Projects

A collection of Python projects focused on data cleaning, automation, and analysis — starting with a CSV Data Cleaner built using pandas.

---

## 🧹 CSV Data Cleaner

### Overview
A beginner-friendly Python program that reads a CSV file, removes duplicate rows, trims whitespace, and reports missing values before saving a cleaned version.

### Features
- Removes duplicate rows
- Trims extra spaces from text fields
- Counts and displays missing values per column
- Saves a cleaned CSV file (`cleaned.csv`)

### How to Run
1. Install Python 3.12+  
2. Install pandas:
   ```bash
   pip install pandas

